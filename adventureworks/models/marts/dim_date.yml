version: 2

models:
  - name: dim_date
    columns:
      - name: date_key
        description: The surrogate key of the date_id
        tests:
          - not_null
          - unique
      
      - name: date_day
        description: The natural key
        tests:
          - not_null
          - unique

      